# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv/

# Database — contains personal financial data, never commit
data/hl_dashboard.duckdb
data/hl_dashboard.duckdb.wal
# Demo database — generated by generate_demo_data.py, no need to track the binary
data/hl_demo.duckdb

# All data imports — contain personal financial data, never commit
data/imports/*.csv
data/imports/**/*.csv
# Exception: dates.csv contains no personal data
!data/imports/dates.csv

# Node / frontend (added when frontend is built)
node_modules/
frontend/dist/

# Logs
logs/
*.log

# Environment files
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Claude Code project instructions — kept local, not for public repo
CLAUDE.md

# Website session data
data/hl_session.json
