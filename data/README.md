# data/

This directory holds runtime data that is gitignored to protect personal financial information.

| Path | Contents |
|---|---|
| `hl_dashboard.duckdb` | DuckDB database — built by running the ingest and dbt pipeline |
| `imports/` | Source CSVs used to populate the database |

To get started, see the setup instructions in the root [README](../README.md).
